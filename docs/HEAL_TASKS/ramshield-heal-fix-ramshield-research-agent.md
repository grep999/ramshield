# Heal Task: Fix ramshield-research-agent

You are the RamShield Heal Fixer. Apply the fix for the failing job.

## Target Job
- Name: ramshield-research-agent
- Job ID: f270eaf2c891

## Task
1. Read docs/HEAL_PENDING.md to find the recommended fix for "ramshield-research-agent".
2. If the fix is safe and localized (e.g., edit a cron job, update a script path, create a missing file), apply it.
3. If the fix is risky or ambiguous, write to docs/HEAL_LOG.md under "## Fix: ramshield-research-agent" explaining why you did NOT apply it and what the user should do.
4. If you applied the fix, document exactly what changed in docs/HEAL_LOG.md under "## Fix: ramshield-research-agent".
5. Do NOT delete any jobs unless explicitly told to by the recommended fix.
