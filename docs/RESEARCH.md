# Research Flow

Automated research findings feeding the pipeline. Populated by the research agent.
Each entry links to a backlog item or roadmap task it informs.

- task_id: roadmap/Q1-eBPF, summary: eBPF/XDP Integration, links: https://github.com/aya-rs/aya, suggested_implementation: Use aya-rs for safe kernel-level packet filtering.
- task_id: roadmap/Q1-eBPF, summary: XDP drop path + aya perf-array → Ramshield batch channel, links: https://aya-rs.github.io/book/, https://docs.rs/aya/latest/aya/, https://github.com/libbpf/libbpf, suggested_implementation: Attach XDP drop program via aya::programs::xdp; forward per-CPU counters through BPF_MAP_TYPE_PERF_EVENT_ARRAY into dedicated tokio task and route into the existing 2M-event batch channel.
- task_id: roadmap/Q2-tflite, summary: Rust ML Inference, links: https://github.com/rust-ml, suggested_implementation: Explore rust-ml/discussion to identify mature ML inference crates for XGBoost as tflite-rust support is limited/stale.
- task_id: roadmap/Q1-linfa, summary: Classical ML Algorithms, links: https://github.com/rust-ml/linfa, https://docs.rs/linfa, suggested_implementation: Integrate linfa crate for classical supervised/unsupervised algorithms as robust Rust alternative to Python scikit-learn.
- task_id: roadmap/WASM-Module-Support, summary: WebAssembly (WASM) Module Support, links: https://wasmtime.dev/, https://github.com/bytecodealliance/wasmtime, suggested_implementation: Integrate wasmtime runtime for sandboxed execution of user-provided WASM modules for custom detection rules.
- task_id: roadmap/Q3-TLS-IPC, summary: TLS 1.3 encrypted IPC using tokio-rustls — pure-Rust, no OpenSSL, TLS 1.3 by default, composable with tokio::net::TcpListener already in axum stack. Links: https://docs.rs/tokio-rustls/latest/tokio_rustls/, https://docs.rs/rustls/latest/rustls/, https://github.com/rustls/tokio-rustls, suggested_implementation: Wrap existing IPC TcpStream with tokio-rustls TlsAcceptor/TlsConnector; load PEM certs via rustls::ServerConfig::builder().with_no_client_auth().with_single_cert(); enable ECH (Encrypted Client Hello) when rustls adds it — track issue #1604.
- task_id: roadmap/WASM-Module-Support, summary: Further research into Rust-based WASM security/detection modules, links: (further web search needed), suggested_implementation: Investigate rust-wasm crates for secure sandboxed execution of user-defined detection logic.
- task_id: roadmap/Q3-iptables, summary: iptables rate limiting integration for RamShield, links: https://github.com/yaa110/rust-iptables, https://man7.org/linux/man-pages/man8/iptables-extensions.8.html, https://github.com/nftables-rs/nftables-rs, suggested_implementation: Wrap `rust-iptables` (libiptc bindings) to atomically APPEND a `-m recent`/`hashlimit` rule on detection trigger; favor `nftables-rs` over libiptc for kernels >=5.10 to avoid shell-out and run via `tokio::task::spawn_blocking`.
- task_id: roadmap/Q3-AuditLog-SHA256, summary: Tamper-evident SHA-256 hash-chained audit log for zero-trust milestone. Pure-Rust stack: `sha2` for digest + `RustCrypto/signatures` (ed25519-dalek) for periodic root-signatures; verifier rebuilds chain by re-hashing and recomputing the running SHA-256 state, then validates nested Ed25519 root per epoch (mirrors AWS QLDB/transcribe-verified-log pattern). Append-only log structured as NDJSON; each line carries `prev_hash` + `event_hash` + `ts`. No new async deps: hash+sig are sync, run via `tokio::task::spawn_blocking`. Links: https://github.com/RustCrypto/signatures, https://docs.rs/sha2/latest/sha2/, https://docs.rs/ed25519-dalek/latest/ed25519_dalek/, suggested_implementation: Add `src/audit.rs` exporting `AppendOnlyLog::append(&event)` + `verify_chain(path)`; persist epoch-signed roots via existing tracing pipeline; keypair loaded from env (PEM) so the same privkey can also seed the TLS node identity used by Q3 tokio-rustls IPC.
- task_id: roadmap/Q1-XGBoost-Scoring, summary: XGBoost threat scoring model + scikit-learn preprocessing pipeline in pure Rust. Pure-Rust inference: quickgrove (XGBoost model loading + fast inference, no C deps), smartcore (XGBoost + full preprocessing: StandardScaler, MinMaxScaler, OneHotEncoder), linfa-preprocessing (StandardScaler, MinMaxScaler, custom scaling, binarize). rust-xgboost provides C bindings but adds C dep. Preprocessing pipeline: linfa-preprocessing::Preprocess trait (standard_scale, min_max_scale, custom_scale, binarize) composable; smartcore::preprocessing mirrors sklearn API. Links: https://github.com/xorq-labs/quickgrove, https://github.com/smartcorelib/smartcore, https://github.com/jtrawinski/linfa-preprocessing, suggested_implementation: Add `src/learning/xgboost.rs` using quickgrove for pure-Rust XGBoost inference + `src/learning/preprocess.rs` wrapping linfa-preprocessing::Preprocess for sklearn-compatible pipeline; load model bytes via include_bytes! at compile-time or runtime file read; expose `score(features: Array2<f32>) -> f32` for RamShield batch channel.
- task_id: roadmap/Q1-Git-LFS, summary: Integrate Git LFS for ML model versioning, links: https://docs.rs/git-lfs, https://github.com/anupcshan/git-lfs-rs, suggested_implementation: Use git-lfs-rs to manage large ML model files within the Git repository, tracking only pointers and storing blobs in LFS storage.
- task_id: roadmap/Q5-IoT-Gateway, summary: IoT Gateway Shielding — Optimized build for ARM/RISC-V edge devices, links: https://github.com/embassy-rs/embassy, https://docs.rs/riscv, https://github.com/rust-embedded/rust-embedded.github.io, suggested_implementation: Use embassy async embedded HAL + riscv crate for bare-metal ARM/RISC-V targets; cross-compile with cargo-embed; build no_std-compatible detection engine via feature flags; size-optimize with panic=abort + lto=fat.