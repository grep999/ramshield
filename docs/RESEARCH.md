- task_id: roadmap/Q5-Heroku-Buildpack, summary: Heroku buildpack for one-click deploy, links: https://devcenter.heroku.com/articles/buildpacks, https://github.com/ramshield/buildpack-example, https://github.com/ramshield/ramshield-buildpack, suggested_implementation: Create a Rust-based buildpack that detects Cargo.toml, compiles the binary with `cargo build --release`, and packages it according to Heroku's buildpack specifications, using existing rust-buildpack patterns or the `buildpack` crate for Rust
- task_id: roadmap/Q5-Kubernetes-Operator, summary: Kubernetes Operator for auto-scaling RamShield deployments, links: https://kube.rs, https://github.com/kube-rs/kube-rs, https://github.com/kube-rs/controller-rs, suggested_implementation: Use `kube` crate (CNCF sandbox, v4.2) with `#[derive(CustomResource)]` to define a `RamShieldCluster` CRD specifying replicas/memory/shards, write a controller reconciler that scales a Deployment and adjusts resource limits via HPA, use `controller-rs` as reference pattern for the reconciliation loop