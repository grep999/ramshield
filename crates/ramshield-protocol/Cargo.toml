[package]
name = "ramshield-protocol"
version = "0.1.0"
edition = "2024"

[lib]
name = "ramshield_protocol"

[dependencies]
serde      = { version = "1", features = ["derive"] }
serde_json = "1"
crc32fast  = "1"

[dev-dependencies]
proptest   = "1"

[lints]
workspace = true