[package]
name = "ramshield-learning"
version = "0.1.0"
edition = "2024"

[features]
default = []
experimental-ml = []

[dependencies]
ramshield-types = { path = "../ramshield-types" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
