[package]
name = "ramshield-metrics"
version = "0.1.0"
edition = "2024"

[dependencies]
ramshield-types = { path = "../ramshield-types" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sysinfo = "0.39"

[lints]
workspace = true
