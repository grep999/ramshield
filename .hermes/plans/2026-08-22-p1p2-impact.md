# P1+P2 Impact Plan (pre-implementation)

## Chosen scope: P1 (EWMA fix + CUSUM + debounce) + P2 (SPOT-lite)
Most efficient pair: both live in the per-IP hot path and the Forecaster's 1s
tick respectively, zero new deps, ~120 lines total. P3/P4/P5 deferred.

## Blast-radius map (verified by grep, not assumed)

### IpRecord gains 2 fields: `cusum_s: f64`, `baseline_rps: f64`
Construction sites — ALL must be updated or compile breaks (good):
- `crates/ramshield-detection/src/lib.rs:453` (merge_record miss branch)
- `crates/ramshield-enforcement/src/lib.rs:253` (block_ip unwrap_or)
- `crates/ramshield-enforcement/src/lib.rs:405` (WAL replay restore)

Downstream readers — additive only, NO wire break:
- `src/ipc/server.rs:355-368` get_status reads ewma_rps/threat_score → untouched
- `src/ipc/server.rs:457-476` GetIpStats builds protocol::IpDetail → field set
  unchanged (cusum stays internal; exposing it = later, YAGNI now)
- dashboard HTML/APIs read metrics counters, not IpRecord fields

Serde: IpRecord derives Serialize/Deserialize — new f64 fields serialize fine,
BUT old WAL/persisted JSON without them fails to deserialize unless
`#[serde(default)]` added. enforcement:405 is the WAL-replay path constructing
records manually, so WAL frames carry explicit values; still add
`#[serde(default)]` on both fields for forward/backward safety of any persisted
snapshots.

### rate_tracker.rs API change
`ewma()` unchanged (still used). NEW: `cusum_step(prev_s, inst, baseline, k)`,
`is_exceeded` call sites in detection gain debounce wrapper.
Callers of `is_exceeded`: detection/lib.rs flush_batch only (grep-verified).

### Debounce ripple
Block condition moves from `should_block || ewma > threshold` (1 sample) to
2-consecutive-exceedance OR cusum-fire. Impact on /tmp/rs99 expectations:
- i01/i02/i03/i06 (rps waves): all sustain ≥4 waves → still block, latency +1 tick max
- i09 re-offender: 8 waves, converges at wave 4-5 → unaffected
- single_ip_burst test asserts NO block → unaffected (fewer blocks = safer direction)
- d-group subnet tests: subnet path untouched by P1

### SPOT-lite ripple (Forecaster.tick_hw)
Replaces `z > anomaly_zscore` gate with `rps > q_0.999` from peaks reservoir;
z-score retained as fallback until reservoir warm (first 60 ticks ≈ 1 min).
Impact: preemptive_block trigger semantics unchanged (same drain_threat_sample).
Config `anomaly_zscore` stays as fallback threshold — no config-schema break.
Metrics: `set_forecast_hw` continues; add nothing (YAGNI).

### Enforcement/WAL
No EnforceCommand changes. Block reasons unchanged ("high_rps", "forecast_anomaly").
WAL replay constructs records with cusum=0/baseline=0 → first post-restart samples
re-baseline naturally (CUSUM S starts at 0 = no accumulated suspicion).

### Test suite (/tmp/rs99) expected deltas
109 checks: expect 109/109 with possible +1-tick latency in i01. No expectation
rewrites anticipated (unlike F1). New unit tests: cusum math (drift catches,
quiet stays 0), debounce (single spike no-block), spot quantile sanity.

## Sequencing (each commit gates-green)
1. storage: +2 fields w/ serde(default); fix 3 construction sites → compile green
2. rate_tracker: cusum_step + unit tests
3. detection merge_record: inst-rate fix (last_flush_ns per record — wait: adds a
   THIRD field; alternative = derive inst from agg span first_ts..last_ts within
   batch, zero storage change) → prefer agg-span derivation, NO new field
4. detection flush_batch: debounce gate
5. forecasting: SPOT-lite reservoir + gate swap
6. /tmp/rs99 full re-run + red_team_full smoke + push

Note on (3): IpAgg already carries first_ts_ns/last_ts_ns → true instantaneous
rate = agg.count / span_secs, computed in flush_batch, passed INTO merge_record.
Removes cumulative-average sawtooth WITHOUT touching IpRecord at all.
